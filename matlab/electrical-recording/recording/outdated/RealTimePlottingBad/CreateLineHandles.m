function lineh = CreateLineHandles(axh,nlines)% lineh = CreateLineHandles(axh,nlines)% Creates a set of evenly-spaced "vertical" lines in the axis% (the length of the lines is 0 to start with)% axh = handle of target axis% nlines = number of lines to create% lineh = vector of line handles (output)xlim = get(axh,'XLim');xcoord = linspace(xlim(1),xlim(2),nlines);ycoord = zeros(1,nlines);lineh = line([xcoord;xcoord],[ycoord;ycoord]);set(lineh,'Color',[ 0 0 1]);
