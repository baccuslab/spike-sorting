function cstar = MMHalfMax(c,r)% MMHalfMax: estimate the concentration at which half-max is attained% cstar = MMHalfMax(c,r)% Interpolates linearly between data pointsrmax = max(r);k = min(find(r >= rmax/2));if (isempty(k))	cstar = Inf;	returnendif (k == 1)	if (r(2) > r(1))		cstar = c(1);	else		cstar = Inf;	% Signal trouble	end	return;endcstar = (rmax/2-r(k-1))/(r(k)-r(k-1))*(c(k)-c(k-1)) + c(k-1);